{
  "kind": "build_request",
  "title": "Populate Bhagavad Gita Chapter 1 with verses 1–72 and complete interpretations",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-33",
      "text": "In `backend/main.mo`, populate the verse dataset for Bhagavad Gita Chapter 1 with all verses 1:1 through 1:72. Each verse must be represented as a `Verse` record and must include non-empty values for every field used by the app: `sanskrit`, `hindiMeaning`, `englishMeaning`, `genZKrishnaInterpretation`, and `actionStep`.",
      "target": "backend",
      "source": {
        "messageIds": [
          "recent-messages-4"
        ],
        "quotes": [
          "In chapter 1 add verses 1-72 with respective interpretation for all columns"
        ]
      },
      "acceptanceCriteria": [
        "Calling `getVersesByChapter(1)` returns a list that contains verses numbered 1 through 72 (no missing verse numbers).",
        "For every verse 1..72 returned for Chapter 1, `sanskrit`, `hindiMeaning`, `englishMeaning`, `genZKrishnaInterpretation`, and `actionStep` are present and not empty strings.",
        "Calling `getVerse(1, n)` returns a non-null result for every `n` from 1 to 72."
      ]
    },
    {
      "id": "REQ-34",
      "text": "Update Chapter 1 metadata returned by `getAllChapters()` in `backend/main.mo` so that `verseCount` for Chapter 1 matches the populated dataset (72).",
      "target": "backend",
      "source": {
        "messageIds": [
          "recent-messages-4"
        ],
        "quotes": [
          "In chapter 1 add verses 1-72 with respective interpretation for all columns"
        ]
      },
      "acceptanceCriteria": [
        "`getAllChapters()` returns Chapter 1 with `verseCount` equal to 72.",
        "The Read Gita Chapter 1 detail view shows the expected number of verses available (verses 1–72 load successfully via the existing queries)."
      ]
    }
  ],
  "constraints": [
    "Backend must remain a single Motoko actor; keep all logic in `backend/main.mo`.",
    "Do not edit any frontend files under `frontend/src/components/ui` or other immutable paths listed in SYSTEM_CONTEXT."
  ],
  "nonGoals": [
    "Adding or updating verses for chapters other than Chapter 1.",
    "Changing the frontend table columns or verse presentation layout.",
    "Introducing external data sources or databases for verse content."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}